import { useRouter } from "next/router";

const Reviews = () => {
    const router = useRouter(query);
    return (
      <div>
        <h1>Reviews</h1>
      </div>
    );
  }
  
  export default Reviews;
  